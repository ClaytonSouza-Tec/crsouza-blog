name: Send Newsletter On Commit

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  send-newsletter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Placeholder step
        run: echo "Newsletter workflow is configured with valid YAML syntax."
